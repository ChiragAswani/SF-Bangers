// Points at the deployed SF Bangers backend. Swap to your machine's LAN IP
// (e.g. "http://192.168.1.23:8080") to test against a local `npm start` in
// the repo root — 127.0.0.1 won't resolve from a physical device/simulator.
export const BACKEND_URL = 'https://sfbangers.com';

export const MOBILE_AUTH_RETURN_URL = 'sfbangers://auth-callback';
